-- Testing the useful-clause check
-- Case: The row "(nat zero zero y) x" is not useful to the rest of the matrix
-- Expected: False

=== data types ===
data Unit where
  tt : Unit

data Nat where
  zero : Nat
  succ : Nat -> Nat

data List where
  nil  : List
  cons : Nat -> List -> List

data OneOfThose where
  nat  : Nat -> Nat -> List -> OneOfThose
  list : List -> OneOfThose
  unit : Unit -> OneOfThose

=== pattern matrix ===
(nat zero zero y) x
(nat x zero y) | (nat zero zero nil)  x
      (list (cons zero nil))          x
            (unit x)                  x
